# from django.contrib import admin    
# from .models import User

# class UserAdmin(admin.ModelAdmin):
#     list_display = ['id', 'email', 'sname', 'dob', 'contact','password','is_superuser']  # Customize the fields to display in the list view
#     search_fields = ['email', 'sname']  # Optional: Enable search functionality

# admin.site.register(User, UserAdmin)
